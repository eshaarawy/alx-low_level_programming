#include "main.h"

/**
 * _memset - copies memory area
 * @s: pointer to the destination
 * @b: pointer to the source
 * @n: bytes from memory
 *
 * Return: dest
*/

char *_memset(char *s, char b, unsigned int n)
{
	return (s);
}
